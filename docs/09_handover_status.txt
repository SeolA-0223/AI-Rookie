[AI-Rookie Auto Handover]
Auto-updated: 2026-05-04 11:51:51 KST

1) Branch Sync
- Current branch: main
- Upstream: origin/main
- Latest commit at update time: c2b92b5 (HEAD -> main, origin/main, origin/HEAD) style: keep guide subtitle on one line
- Latest CI run: CI: completed/success (https://github.com/SeolA-0223/AI-Rookie/actions/runs/25298372060)
- PR link: (not applicable on main)

2) Working Tree
- Clean: no
- git status --short --branch
- ## main...origin/main
- M  frontend/src/styles.css

3) Recent Commits (max 8)
- c2b92b5 (HEAD -> main, origin/main, origin/HEAD) style: keep guide subtitle on one line
- 41a47db refactor: simplify guide header and top navigation
- 7434f7f style: separate sidebar title from navigation
- 71a2deb style: space ordinance draft note from preview
- 3806fe3 style: add spacing before source provenance details
- b241750 style: align sidebar with binder sheet
- 8a86208 refactor: simplify sidebar navigation controls
- 2cf9fbf fix: add source-localize vercel entrypoint

4) Staged Files
- frontend/src/styles.css

5) Unstaged Files
- (none)

6) Untracked Files
- (none)

7) Manual Notes
### [MANUAL NOTES START]
- `law-go-public` provider was added. It searches with official `law.go.kr` DRF and fetches clauses through the public `ordinInfoP.do` + `ordinJoListRInc_XML.do` + `ordinBdyPrint.do` flow.
- Local verification succeeded against `law.go.kr`: ordinance search returned real candidates, `source-status?provider=law-go-public` reported `enabled=true`, and a local fetch for ordinance `1840747` returned 28 parsed clauses.
- Live Vercel behavior changed again after commit `63adfad`. As of 2026-04-10, `https://ai-rookie-three.vercel.app/api/source-search?provider=law-go-public...` returns real results with `searchBackend=drf+html`, and `/api/analyze` with `law-go-public` now succeeds for both same-version IDs (`1840747 -> 1840747`) and historical/current pairs (`1811191 -> 2054497`).
- `law-go-public` search now expands a single strong seed result with `ordinHstListR.do` history entries. Mock integration coverage proves that `/source-search` can now return a recommended pre/post pair from public ordinance history when the search seed is available.
- `law-go-public` now merges DRF + HTML candidates, re-ranks them by query match, and retries a body-title-only query variant for wide-area searches such as `서울특별시 청년 기본 조례`.
- `law-go-public` detail fetch now follows the live page flow more closely: it reads `gubun`, `nwYn`, `ancYd`, and `ancNo` from `ordinInfoP.do`, then reuses those values for `ordinJoListRInc_XML.do` and `ordinBdyPrint.do`. This fixed live historical/current analysis on Vercel.
- Even after the re-ranking change, the default demo `LAW_GO_OC=test` still does not reliably return the exact metropolitan-city ordinance for some queries. As of 2026-04-10, `서울특별시 청년 기본 조례` still comes back with district ordinances first in real local verification.
- `local-fixture` now supports bundled `caseId` selection. `/case-catalog` exposes the three municipality case packs, and the dashboard can switch case packs without editing `data/samples/*`.
- `npm run check` passes after the new provider work. Current eval summary remains `evaluatedCaseCount=3`, `passedCaseCount=3`, `allCasesPassed=true`.
- Live `korea-law-mcp` verification is still open. Public README evidence exists, but no reachable public HTTP deployment was found, and this workspace currently lacks a usable Python/uv runtime to launch the upstream server locally.
- Next implementation order: secure a real `LAW_GO_OC` or alternate live search runtime -> decide between curated fallback vs search diagnostics for exact-title misses -> secure a real MCP runtime for contract verification -> tighten the temporary UI around search/result provenance.
### [MANUAL NOTES END]

8) Quick Commands
- status: git status --short --branch
- check: npm run check
- smoke: npm run smoke
- manual refresh: npm run handover:update
