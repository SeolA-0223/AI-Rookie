# Output Notes

AI-Rookie delivers real artifacts across `frontend/`, `backend/`, `shared/`, and `docs/`.

The root `output/` directory is reserved for harness-generated evidence such as:

- screenshots or demo captures
- exported comparison notes
- task-specific deliverable summaries

For the current task, the main deliverables stay in the repository code and are tracked by `SPEC.md`, `SELF_CHECK.md`, and `QA_REPORT.md`.
